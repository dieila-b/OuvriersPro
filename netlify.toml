# netlify.toml
[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

# Proxy /api/payments/start → la Netlify Function "payments-start"
[[redirects]]
  from = "/api/payments/start"
  to = "/.netlify/functions/payments-start"
  status = 200
